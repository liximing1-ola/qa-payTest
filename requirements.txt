# ============================================
# 依赖清单（与 CI 及本地实测运行集一致）
#
# 核心段为离线检查与用例收集链的最小集合，
# 版本与 .github/workflows/ci.yml 锚定，请勿单独漂移；
# 可选段仅特定独立脚本需要，不影响测试主链。
# 安装：pip install -r requirements.txt
# ============================================

# ---- 核心（CI 同款，py3.11 实测可构建/运行）----
pytest==7.2.1
requests==2.28.1
urllib3==1.26.13
PyMySQL==1.0.2
gevent==22.10.2
PyYAML==6.0.1
redis==8.1.0

# ---- 可选（独立脚本依赖，不影响 pytest 主链）----
# others/bot_dinner.py、others/bot_tea.py（节假日判断）
chinesecalendar>=1.10.0
# autoGitPull.py（git 拉取通知）
GitPython>=3.1.20
